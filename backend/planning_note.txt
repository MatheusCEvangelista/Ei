=== SQL — executar no Supabase ===

CREATE TABLE planning_sessions (
  id         uuid default gen_random_uuid() primary key,
  user_id    uuid references auth.users(id) on delete cascade,
  month      integer not null,
  year       integer not null,
  items      jsonb default '[]',
  leon_notes text,
  created_at timestamptz default now(),
  updated_at timestamptz default now(),
  UNIQUE(user_id, month, year)
);
ALTER TABLE planning_sessions ENABLE ROW LEVEL SECURITY;
CREATE POLICY "próprio" ON planning_sessions FOR ALL USING (auth.uid() = user_id);

=== server.js — adicionar ===

const planningRoutes = require('./routes/planning');
app.use('/api/planning', planningRoutes);

=== App.jsx — adicionar rota ===

import PlanningPage from './pages/PlanningPage';
<Route path="/planning" element={<PrivateRoute><PlanningPage/></PrivateRoute>} />

=== Navbar.jsx — adicionar em SECONDARY_GROUPS > Análise ===

{ to:'/planning', label:'Planejamento', icon:'🗺️' },
